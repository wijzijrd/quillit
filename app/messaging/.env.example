PORT=3003
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
# Shared secret checked on incoming internal connectrpc calls (from auth);
# must match auth's INTERNAL_RPC_SECRET. Generate with: openssl rand -hex 32
INTERNAL_RPC_SECRET=replace-with-a-long-random-string
